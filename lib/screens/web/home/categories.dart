import 'package:flutter/cupertino.dart';

class WebCategoryList extends StatelessWidget {
  const WebCategoryList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
